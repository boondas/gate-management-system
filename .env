# Database URL — NO password needed because you have "trust"
DATABASE_URL=postgresql://postgres@localhost/gate_management

# If your app uses SQLALCHEMY_DATABASE_URI instead
SQLALCHEMY_DATABASE_URI=postgresql://postgres@localhost/gate_management

# Flask settings
FLASK_APP=main.py          # ← change only if your file is named differently (e.g. run.py)
FLASK_ENV=development
FLASK_DEBUG=1
SQLALCHEMY_TRACK_MODIFICATIONS=False